<!DOCTYPE html>
<html>
<head>
  <title>Formulario de registro</title>
</head>
<body>
  <h1>Formulario de registro</h1>
  <form action="/register" method="post" enctype="multipart/form-data">
    <label for="fullName">Nombre completo:</label><br>
    <input type="text" id="fullName" name="fullName" value="<%= locals.oldData ? oldData.fullName : null %>"><br>
    <!-- Entra si vino errors en la variable local y si existe la propiedad nombre en errors -->
    <% if(locals.errors && errors.fullName) {%> 
      <div class="text-danger">
          <%= errors.fullName.msg %>
      </div>
  <% } %> 
   
    <label for="email">Correo electrónico:</label><br>
    <input type="text" id="email" name="email" value="<%= locals.oldData ? oldData.email : null %>"><br>
    <% if(locals.errors && errors.email) {%> 
      <div class="text-danger">
          <%= errors.email.msg %>
      </div>
  <% } %> 
    

    <label for="password">Contraseña:</label><br>
    <input type="password" id="password" name="password" value="<%= locals.oldData ? oldData.password : null %>"><br>
    <% if(locals.errors && errors.password) {%> 
      <div class="text-danger">
          <%= errors.password.msg %>
      </div>
  <% } %> 

    <label for="country">País:</label><br>
    <select id="country" name="country">
      <option value="AR">Argentina</option>
      <option value="CL">Chile</option>
      <option value="PE">Perú</option>
      <option value="BR">Brasil</option>
    </select><br><br>
  
    

    <label for="avatar">Imagen de perfil:</label><br>
    <input type="file" id="avatar" name="avatar"><br><br>

    <input type="submit" value="Registrarse">
  </form> 
</body>
</html>





